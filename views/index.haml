%h1 Images
%table
  %thead
    %tr
      %th Image
      %th Flagged?
  %tbody
    - @images.each_with_index do |image, row|
      %tr{:id => image.id, :class => (row % 2) == 0 ? 'even' : ''}
        %td
          %img{:src => image.image_url, :class => 'preview'}
        %td= image.flagged
%p
  %a{:href => '/images/new'} Create New Image